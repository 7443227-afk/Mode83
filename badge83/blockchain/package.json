{
  "name": "badge83-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Preuve d'ancrage EVM optionnelle pour Badge83.",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:sepolia:v2": "hardhat run scripts/deploy-v2.js --network sepolia",
    "deploy:registry:local": "hardhat run scripts/deploy-registry.js --network localhost",
    "deploy:registry:sepolia": "hardhat run scripts/deploy-registry.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  }
}